-- =============================================================================
-- Tenant Migration (down): 079_attendance_overtime_status_pending_approval
-- =============================================================================
-- Postgres no-op — lihat keterangan pada file up. Tidak ada perubahan skema
-- yang harus di-revert.

SELECT 1;
